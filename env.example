# Reloop Mail Server Configuration
# =================================

# Domain Configuration
# --------------------
# Your primary domain (e.g., yourdomain.com)
DOMAIN=yourdomain.com

# Mail Server Hostname (auto-detected from hostname command, or set manually)
# Leave empty to auto-detect from hostname, or set to override (e.g., mail.yourdomain.com)
MAIL_HOSTNAME=

# Database Configuration
# ----------------------
# External PostgreSQL Database Configuration
# Set DB_HOST to your remote PostgreSQL server IP or hostname
# Example: DB_HOST=192.168.1.100 or DB_HOST=db.yourdomain.com
DB_HOST=your_remote_postgresql_host
DB_NAME=reloop_mail
DB_USER=reloop_user
DB_PASSWORD=reloop_secure_password_2024
# Make sure your remote PostgreSQL is accessible on port 5432 (or configure firewall accordingly)

# Redis Configuration
# -------------------
REDIS_PASSWORD=reloop_redis_password_2024
REDIS_PORT=127.0.0.1:26379

# Mail Ports Configuration
# ------------------------
SMTP_PORT=25
SMTPS_PORT=465
SUBMISSION_PORT=587
IMAP_PORT=143
IMAPS_PORT=993
POP_PORT=110
POPS_PORT=995

# Timezone
# --------
TZ=UTC

# Additional Domain Configuration
# ------------------------------
# Comma-separated list of additional domains to handle
# Example: ADDITIONAL_DOMAINS=domain2.com,domain3.com
ADDITIONAL_DOMAINS=

# Optional: Custom ports (uncomment to use different ports)
# SMTP_PORT=1025
# IMAP_PORT=10143
