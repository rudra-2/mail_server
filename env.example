# Reloop Mail Server Configuration
# =================================

# Mail Server Hostname (change this to your domain)
MAIL_HOSTNAME=mail.yourdomain.com

# Database Configuration
# ----------------------
# External PostgreSQL Database Configuration
# Set DB_HOST to your remote PostgreSQL server IP or hostname
# Example: DB_HOST=192.168.1.100 or DB_HOST=db.yourdomain.com
DB_HOST=your_remote_postgresql_host
DB_NAME=reloop_mail
DB_USER=reloop_user
DB_PASSWORD=reloop_secure_password_2024
# Make sure your remote PostgreSQL is accessible on port 5432 (or configure firewall accordingly)

# Redis Configuration
# -------------------
REDIS_PASSWORD=reloop_redis_password_2024
REDIS_PORT=127.0.0.1:26379

# Mail Ports Configuration
# ------------------------
SMTP_PORT=25
SMTPS_PORT=465
SUBMISSION_PORT=587
IMAP_PORT=143
IMAPS_PORT=993
POP_PORT=110
POPS_PORT=995

# Timezone
# --------
TZ=UTC

# Optional: Custom ports (uncomment to use different ports)
# SMTP_PORT=1025
# IMAP_PORT=10143
