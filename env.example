# Reloop Mail Server Configuration
# =================================

# Mail Server Hostname (change this to your domain)
MAIL_HOSTNAME=mail.yourdomain.com

# Database Configuration
# ----------------------
# If using external PostgreSQL, set DB_HOST to your external database
# If using local PostgreSQL (included in docker-compose), leave DB_HOST empty
DB_HOST=
DB_NAME=reloop_mail
DB_USER=reloop_user
DB_PASSWORD=reloop_secure_password_2024
DB_PORT=127.0.0.1:5432

# Redis Configuration
# -------------------
REDIS_PASSWORD=reloop_redis_password_2024
REDIS_PORT=127.0.0.1:26379

# Mail Ports Configuration
# ------------------------
SMTP_PORT=25
SMTPS_PORT=465
SUBMISSION_PORT=587
IMAP_PORT=143
IMAPS_PORT=993
POP_PORT=110
POPS_PORT=995

# Web Ports Configuration
# -----------------------
HTTP_PORT=80
HTTPS_PORT=443

# Timezone
# --------
TZ=UTC

# Optional: Custom ports (uncomment to use different ports)
# SMTP_PORT=1025
# IMAP_PORT=10143
# HTTP_PORT=8080
# HTTPS_PORT=8443
